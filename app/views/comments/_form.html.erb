<%= form_for ([@family, @family.comments.build]) do |f| %>
<% if @family.errors.any? %>
    <div id="alert-danger">
      <h2><%= pluralize(@family.errors.count, "error") %> prohibited this comment from being saved:</h2>
      <ul>
      <% @family.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<%= f.text_area :content, class:"single-input", placeholder:"enter comment"%>
<%= f.submit "Add comment", class:"genric-btn info-border circle" %>
<% end %>
